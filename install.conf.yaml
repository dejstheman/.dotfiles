- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.alias:
      force: true
      path: .alias
    ~/.gitconfig:
      force: true
      path: .gitconfig
    ~/.gitignore:
      force: true
      path: .gitignore
    ~/.zshrc:
      force: true
      path: .zshrc
    ~/.git-template/:
      force: true
      glob: true
      create: true
      path: .git-template/*

- shell:
    - # homebrew
      command: brew bundle --file=./Brewfile
      stdout: true
      stderr: true

    - # oh-my-zsh
      command: ./.oh-my-zsh.sh
      stdout: true
      stderr: true
